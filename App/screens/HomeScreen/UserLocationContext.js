import { createContext } from "react";

export const UserLocationContext = createContext(null);

//js creado para poder utilizar las variables location y la funcion setlocation en todas las pantallas.